#include <stdio.h>

int main(void)
{
	int x = 10;
	int y = 20;

	printf("It's never too late./n It never rains but it poors");
	printf("%d", x);
	printf("%d", x + y);
	printf("%d + %d = %d", x, y, x + y);
	printf("%d * %d = %d", x, y, x * y);
	printf("*\n**\n***\n****\n");

	return 0;
}